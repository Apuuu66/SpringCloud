server:
  port: 8087
spring:
  application:
    name: config-client-p6spy
  cloud:
    config:
      profile: dev
      label: master
      discovery:
        enabled: true
        service-id: config-server
#  jpa:
#    show-sql: true
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8762/eureka/

